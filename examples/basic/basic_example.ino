#include <ESPWifiManager.h>

void setup() {
    // your setup
}

void loop() {
    if(ESPWifiManager.isConnected()) {
        // your logic here
    }
    // if you're here, AP mode is currently active
}